name: Build RedSquare Screens (Android TV)

on:
  repository_dispatch:
    types: [trigger-screens-android-tv-build]
  workflow_dispatch:
    inputs:
      build_id:
        description: 'ID of the build record in the database'
        required: true
      version:
        description: 'Version for this build'
        required: true
        default: '1.0.0-manual'

env:
  VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
  VITE_SUPABASE_ANON_KEY: ${{ secrets.VITE_SUPABASE_ANON_KEY }}
  VITE_SUPABASE_PROJECT_ID: ${{ secrets.VITE_SUPABASE_PROJECT_ID }}
  VITE_MAPBOX_PUBLIC_TOKEN: ${{ secrets.VITE_MAPBOX_PUBLIC_TOKEN }}
  VITE_STRIPE_PUBLISHABLE_KEY: ${{ secrets.VITE_STRIPE_PUBLISHABLE_KEY }}
  SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
  SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
  MAPBOX_PUBLIC_TOKEN: ${{ secrets.VITE_MAPBOX_PUBLIC_TOKEN }}
  STRIPE_SECRET_KEY: ${{ secrets.STRIPE_SECRET_KEY }}
  RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
  HUGGING_FACE_ACCESS_TOKEN: ${{ secrets.HUGGING_FACE_ACCESS_TOKEN }}
  GH_ACCESS_TOKEN: ${{ secrets.GH_ACCESS_TOKEN }}
  GH_REPO_OWNER: ${{ secrets.GH_REPO_OWNER }}
  GH_REPO_NAME: ${{ secrets.GH_REPO_NAME }}

jobs:
  build-screens-android-tv:
    runs-on: ubuntu-latest
    steps:
      - name: Get Build Info from Dispatch
        id: build_info
        run: |
          if [ -n "${{ github.event.client_payload.build_id }}" ]; then
            echo "build_id=${{ github.event.client_payload.build_id }}" >> $GITHUB_OUTPUT
            echo "version=${{ github.event.client_payload.version }}" >> $GITHUB_OUTPUT
          else
            echo "build_id=${{ github.event.inputs.build_id }}" >> $GITHUB_OUTPUT
            echo "version=${{ github.event.inputs.version }}" >> $GITHUB_OUTPUT
          fi

      - name: Update Build Status to 'In Progress'
        run: |
          SUPABASE_URL="${{ secrets.SUPABASE_URL }}"
          if [ -z "$SUPABASE_URL" ]; then
            echo "Error: SUPABASE_URL secret not set"
            exit 1
          fi
          curl -X POST "${SUPABASE_URL}/functions/v1/update-build-status" \
            -H "Authorization: Bearer ${{ secrets.GH_ACTION_SECRET }}" \
            -H "Content-Type: application/json" \
            -d '{
              "build_id": "${{ steps.build_info.outputs.build_id }}",
              "status": "in_progress",
              "logs_url": "${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}",
              "commit_hash": "${{ github.sha }}"
            }'

      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Dependencies
        run: npm install

      - name: Setup Android Platform
        run: |
          if [ ! -d "android" ]; then
            npx cap add android
          fi
          npx cap sync android

      - name: Build Web App
        run: npm run build:mobile
        env:
          VITE_APP_TYPE: screens
          VITE_TV_OPTIMIZED: 'true'
          VITE_CAPACITOR: 'true'

      - name: Validate build assets
        run: |
          echo "Checking for absolute asset paths in build..."
          if grep -r '"/assets' dist/ 2>/dev/null; then
            echo "❌ Found absolute /assets paths in build - this will cause issues in Capacitor apps"
            echo "Build artifacts should use relative paths for compatibility"
            exit 1
          fi
          echo "✅ Build validation passed - no absolute asset paths found"

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Setup Capacitor Android Platform
        run: |
          # Ensure Capacitor is initialized with correct config
          echo "Verifying Capacitor configuration..."
          if [ ! -f "capacitor.config.ts" ]; then
            echo "ERROR: Capacitor config not found"
            exit 1
          fi
          
          # Verify the correct app ID is set
          if ! grep -q "com.redsquare.screens" capacitor.config.ts; then
            echo "ERROR: Incorrect app ID in capacitor.config.ts"
            exit 1
          fi
          
          # Check if Android platform exists and is complete
          if [ ! -f "android/gradlew" ] || [ ! -f "android/build.gradle" ]; then
            echo "Android project incomplete, removing and re-adding platform"
            rm -rf android
            npx cap add android
            
            # Verify the platform was added successfully
            if [ ! -f "android/gradlew" ]; then
              echo "ERROR: Failed to create Android platform"
              exit 1
            fi
            
            # Set proper permissions for gradlew
            chmod +x android/gradlew
          else
            echo "Android platform properly configured, ensuring gradlew permissions"
            chmod +x android/gradlew
          fi
          
          echo "✅ Android platform setup completed"

      - name: Sync Capacitor Project
        run: |
          npx cap sync android
          
          # Ensure gradlew is executable
          if [ -f "android/gradlew" ]; then
            chmod +x android/gradlew
            echo "Android gradlew found and made executable"
          else
            echo "ERROR: gradlew still not found after sync"
            exit 1
          fi

      - name: Configure Android Build for TV
        run: |
          # Configure for Android TV and ensure it loads broadcast app by default
          if [ -f "android/app/src/main/AndroidManifest.xml" ]; then
            echo "Configuring Android TV manifest..."
            
            # Create Android TV banner asset
            mkdir -p android/app/src/main/res/drawable-xhdpi
            echo "iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mP8/5+hHgAHggJ/PchI7wAAAABJRU5ErkJggg==" | base64 -d > android/app/src/main/res/drawable-xhdpi/banner.png
            
            # Remove touchscreen requirement for TV
            sed -i 's/android:name="android.hardware.touchscreen" android:required="true"/android:name="android.hardware.touchscreen" android:required="false"/' android/app/src/main/AndroidManifest.xml || echo "Touchscreen already configured"
            
            # Add LEANBACK_LAUNCHER category for Android TV
            if ! grep -q "android.intent.category.LEANBACK_LAUNCHER" android/app/src/main/AndroidManifest.xml; then
              sed -i '/<category android:name="android.intent.category.LAUNCHER"\/>/a\                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />' android/app/src/main/AndroidManifest.xml
            fi
            
            # Add TV banner
            if ! grep -q 'android:banner=' android/app/src/main/AndroidManifest.xml; then
              sed -i 's/<application/& android:banner="@drawable\/banner"/' android/app/src/main/AndroidManifest.xml
            fi
            
            # Add leanback feature requirement and configure as screen application
            if ! grep -q 'android:name="android.software.leanback"' android/app/src/main/AndroidManifest.xml; then
              sed -i '/<\/manifest>/i\    <uses-feature android:name="android.software.leanback" android:required="true" />' android/app/src/main/AndroidManifest.xml
              sed -i '/<\/manifest>/i\    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />' android/app/src/main/AndroidManifest.xml
            fi
            
            # Set default intent to open broadcast app directly
            if ! grep -q 'android.intent.action.VIEW' android/app/src/main/AndroidManifest.xml; then
              sed -i '/<\/activity>/i\                <intent-filter>\n                    <action android:name="android.intent.action.VIEW" />\n                    <category android:name="android.intent.category.DEFAULT" />\n                    <category android:name="android.intent.category.BROWSABLE" />\n                    <data android:scheme="redsquare" android:host="broadcast" />\n                </intent-filter>' android/app/src/main/AndroidManifest.xml
            fi
            
            echo "Android TV configuration completed"
          else
            echo "AndroidManifest.xml not found, checking android directory structure..."
            find android -name "AndroidManifest.xml" || echo "No AndroidManifest.xml found anywhere"
          fi

      - name: Create Android Keystore
        env:
          KEYSTORE_BASE64: ${{ secrets.ANDROID_SIGNING_KEY_BASE64 }}
        run: |
          if [ ! -z "$KEYSTORE_BASE64" ]; then
            echo "$KEYSTORE_BASE64" | base64 --decode > android/app/release.keystore
          else
            echo "No keystore provided, using debug signing"
          fi

      - name: Build RedSquare Screens Android TV APK
        run: |
          cd android
          
          # Ensure gradlew has proper permissions
          chmod +x ./gradlew
          
          # Check Gradle and Java versions for debugging
          ./gradlew --version
          java -version
          
          # Clean previous builds
          ./gradlew clean
          
          # Build the release APK
          echo "Starting assembleRelease build..."
          ./gradlew assembleRelease --stacktrace --info
          
          echo "Build completed. Checking output files..."
          find . -name "*.apk" -type f -exec ls -la {} \;
          
          # Verify APK was created
          if [ ! -f "app/build/outputs/apk/release/app-release-unsigned.apk" ] && [ ! -f "app/build/outputs/apk/release/app-release.apk" ]; then
            echo "❌ APK build failed - no release APK found"
            echo "Available APK files:"
            find . -name "*.apk" -type f || echo "No APK files found"
            echo "Contents of outputs directory:"
            ls -la app/build/outputs/ 2>/dev/null || echo "No outputs directory"
            exit 1
          fi
          
          echo "✅ APK build completed successfully"
        env:
          SIGNING_KEY_ALIAS: ${{ secrets.ANDROID_SIGNING_KEY_ALIAS }}
          SIGNING_KEY_PASSWORD: ${{ secrets.ANDROID_SIGNING_KEY_PASSWORD }}
          SIGNING_STORE_PASSWORD: ${{ secrets.ANDROID_SIGNING_STORE_PASSWORD }}

      - name: Upload APK as Artifact
        uses: actions/upload-artifact@v4
        with:
          name: screens-android-tv-app
          path: android/app/build/outputs/apk/release/*.apk

      - name: Upload APK to Supabase Storage
        id: upload_apk
        run: |
          # Find the actual APK file
          FILE_PATH=$(find android/app/build/outputs/apk/release/ -name "*.apk" | head -1)
          if [ -z "$FILE_PATH" ]; then
            echo "No APK file found in release directory"
            exit 1
          fi
          echo "Found APK: $FILE_PATH"
          
          FILE_NAME="screens-android-tv-app-v${{ steps.build_info.outputs.version }}.apk"
          SUPABASE_BUCKET="tv-files"

          # Get the public URL of the bucket
          BUCKET_URL="${{ secrets.SUPABASE_URL }}/storage/v1/object/public/${SUPABASE_BUCKET}/${FILE_NAME}"
          echo "artifact_url=$BUCKET_URL" >> $GITHUB_OUTPUT

          curl -X POST \
            "${{ secrets.SUPABASE_URL }}/storage/v1/object/${SUPABASE_BUCKET}/${FILE_NAME}" \
            -H "Authorization: Bearer ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}" \
            -H "Content-Type: application/vnd.android.package-archive" \
            --data-binary @"$FILE_PATH"

      - name: Update Build Status to 'Success'
        if: success()
        run: |
          curl -X POST "${{ secrets.SUPABASE_URL }}/functions/v1/update-build-status" \
            -H "Authorization: Bearer ${{ secrets.GH_ACTION_SECRET }}" \
            -H "Content-Type: application/json" \
            -d '{
              "build_id": "${{ steps.build_info.outputs.build_id }}",
              "status": "success",
              "artifact_url": "${{ steps.upload_apk.outputs.artifact_url }}"
            }'

      - name: Update Build Status to 'Failed'
        if: failure() || cancelled()
        run: |
          STATUS="failed"
          if [ "${{ job.status }}" == "cancelled" ]; then
            STATUS="cancelled"
          fi
          curl -X POST "${{ secrets.SUPABASE_URL }}/functions/v1/update-build-status" \
            -H "Authorization: Bearer ${{ secrets.GH_ACTION_SECRET }}" \
            -H "Content-Type: application/json" \
            -d '{
              "build_id": "${{ steps.build_info.outputs.build_id }}",
              "status": "'$STATUS'"
            }'